server:
  port: 9002

spring:
  application:
    name: eureka-provider   # spring.application.name相同则可以使用Ribbon


eureka:
  instance:
    preferIpAddress: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:9000/eureka/
